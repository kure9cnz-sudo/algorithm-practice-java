package greedy;

/**
 * 题目目标：用贪心策略选择最多数量的互不重叠区间。
 *
 * <p>输入：区间数组 intervals，每个区间格式为 {start, end}，表示半开区间 [start, end)。</p>
 * <p>输出：最多可选择的互不重叠区间数量。end <= nextStart 视为不重叠。</p>
 * <p>异常：输入为 null、区间不是两个端点、start > end 时抛出 IllegalArgumentException。</p>
 */
public final class IntervalScheduling {
    private IntervalScheduling() {
    }

    public static int maxNonOverlappingIntervals(int[][] intervals) {
        throw new UnsupportedOperationException("TODO: implement by yourself");
    }
}
