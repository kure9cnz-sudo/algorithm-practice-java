package search;

/**
 * 题目目标：在升序数组中找到第一个大于目标值的位置。
 *
 * <p>输入：升序 int 数组和目标值。数组为 null 时应抛出 IllegalArgumentException。</p>
 * <p>输出：第一个满足 sorted[i] > target 的下标；若不存在，返回 sorted.length。</p>
 */
public final class UpperBound {
    private UpperBound() {
    }

    /**
     * 返回 upper bound 下标。
     *
     * @param sorted 升序数组
     * @param target 目标值
     * @return 第一个大于 target 的位置
     */
    public static int upperBound(int[] sorted, int target) {
        throw new UnsupportedOperationException("TODO: implement by yourself");
    }
}
