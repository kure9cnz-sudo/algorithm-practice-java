package search;

/**
 * 题目目标：在升序数组中用二分查找寻找目标值。
 *
 * <p>输入：升序 int 数组和目标值。数组为 null 时应抛出 IllegalArgumentException。</p>
 * <p>输出：目标值所在下标；不存在时返回 -1。若有重复元素，返回任意一个目标值下标即可。</p>
 */
public final class BinarySearch {
    private BinarySearch() {
    }

    /**
     * 查找目标值下标。
     *
     * @param sorted 升序数组
     * @param target 目标值
     * @return 目标值下标，不存在时为 -1
     */
    public static int indexOf(int[] sorted, int target) {
        throw new UnsupportedOperationException("TODO: implement by yourself");
    }
}
