package hashtable;

import java.util.Map;

/**
 * 题目目标：用哈希表统计字符串中每个字符出现次数。
 *
 * <p>输入：字符串 text，可为空字符串，但不可为 null。</p>
 * <p>输出：字符到出现次数的映射。大小写不同字符应分别统计，空格等普通字符也应统计。</p>
 * <p>异常：text 为 null 时抛出 IllegalArgumentException。</p>
 */
public final class FrequencyCounter {
    private FrequencyCounter() {
    }

    public static Map<Character, Integer> countFrequencies(String text) {
        throw new UnsupportedOperationException("TODO: implement by yourself");
    }
}
