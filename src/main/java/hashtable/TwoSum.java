package hashtable;

/**
 * 题目目标：用哈希表寻找数组中和为目标值的两个下标。
 *
 * <p>输入：整数数组 nums 和目标值 target。nums 为 null 时抛出 IllegalArgumentException。</p>
 * <p>输出：长度为 2 的下标数组 {i, j}，要求 i < j 且 nums[i] + nums[j] == target；不存在时返回空数组。</p>
 */
public final class TwoSum {
    private TwoSum() {
    }

    public static int[] findIndices(int[] nums, int target) {
        throw new UnsupportedOperationException("TODO: implement by yourself");
    }
}
